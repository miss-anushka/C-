#include <iostream>
#include <stdio.h>
using namespace std;
int main(){
    printf("hello C!!\n"); //function printf is called
    cout<<"hello C++!!"<<endl; //object cout is used
    struct A{
        int age; // struct is used (by default public)
    };
    class B{
        int age; //class is used (by default private)
    };
    
return 0;

}